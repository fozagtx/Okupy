export const dashboardHtml = `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Okupy — leave the build cave</title>
<style>
:root{--ink:#151515;--paper:#f4f0e6;--acid:#d9ff43;--purple:#7057ff;--warn:#ff5b5b;--good:#0a7c2f}*{box-sizing:border-box}body{margin:0;background:var(--paper);color:var(--ink);font:16px/1.45 ui-monospace,SFMono-Regular,Menlo,monospace}.shell{max-width:1080px;margin:auto;padding:28px}.top{display:flex;justify-content:space-between;align-items:center;border-bottom:2px solid;padding-bottom:18px}.brand{font:bold 25px system-ui}.pill{background:var(--acid);border:1px solid;border-radius:99px;padding:7px 12px;font-size:12px}.hero{display:grid;grid-template-columns:1.4fr .6fr;gap:30px;padding:70px 0 55px}.hero h1{font:800 clamp(44px,8vw,88px)/.92 system-ui;margin:0;letter-spacing:-.06em}.hero h1 span{color:var(--purple)}.hero p{font:18px/1.5 system-ui;margin-top:24px;max-width:640px}.stat{border:2px solid;padding:22px;align-self:end;background:white;box-shadow:7px 7px 0 var(--ink)}.grid{display:grid;grid-template-columns:1fr 1fr;gap:20px}.card{border:1.5px solid;padding:25px;background:#faf8f2}.card h2{font:700 22px system-ui;margin-top:0}label{display:block;font-size:12px;text-transform:uppercase;margin:14px 0 6px}input,textarea{width:100%;border:1.5px solid;padding:12px;background:white;font:inherit}textarea{min-height:75px}button{border:1.5px solid;background:var(--ink);color:white;padding:13px 18px;margin-top:16px;font:bold 14px inherit;cursor:pointer}button.alt{background:var(--purple)}button.ghost{background:var(--paper);color:var(--ink)}#status{white-space:pre-wrap;margin-top:14px;font-size:13px}.reminder{border:1.5px solid;padding:14px;margin-top:12px;background:white;display:flex;justify-content:space-between;align-items:center;gap:14px}.reminder b{display:block;font:14px system-ui}.reminder time{font-size:12px;color:var(--purple)}.reminder button{margin:0;padding:8px 12px;font-size:12px;background:var(--paper);color:var(--ink)}.watch-item{border:1.5px solid;padding:14px;margin-top:12px;background:white;display:grid;grid-template-columns:60px 1fr auto;gap:14px;align-items:center}.watch-item img{width:60px;height:60px;object-fit:contain;border:1px solid var(--ink);background:white}.watch-item .info b{display:block;font:14px system-ui;margin-bottom:4px}.watch-item .info .meta{font-size:12px;color:var(--purple)}.watch-item .info .price{font-size:18px;font-weight:700;margin-top:4px}.watch-item .info .price.drop{color:var(--warn)}.watch-item .info .price.target-hit{color:var(--good)}.watch-item .info .target{font-size:12px;margin-top:2px;color:#444}.watch-item .info .alert{font-size:12px;margin-top:4px;color:var(--warn);font-weight:700}.watch-item .actions{display:flex;flex-direction:column;gap:6px}.watch-item .actions button{margin:0;padding:6px 10px;font-size:11px;background:var(--paper);color:var(--ink)}.watch-empty{padding:14px;margin-top:12px;border:1.5px dashed var(--ink);background:#fff;font-size:13px;text-align:center}.cart-controls{display:flex;gap:10px;align-items:center;margin-top:12px}.cart-controls button{margin:0;padding:8px 12px;font-size:12px}.steps{display:flex;gap:12px;margin:55px 0 25px}.steps div{flex:1;border-top:2px solid;padding-top:12px}.steps b{display:block;color:var(--purple)}@media(max-width:720px){.hero,.grid{grid-template-columns:1fr}.hero{padding:45px 0}.steps{display:block}.steps div{margin:20px 0}.watch-item{grid-template-columns:60px 1fr}}
</style></head><body><main class="shell"><header class="top"><div class="brand">OKUPY /</div><div class="pill">● iMessage-first agent</div></header>
<section class="hero"><div><h1>Free food.<br><span>Real people.</span><br>Keep building.</h1><p>Tell Okupy what you're making once. Your agent remembers, searches Firecrawl for nearby events, and texts you the rooms with food, cofounders, customers, feedback, and credits. Ask it to remind you and it will ping you back on iMessage.</p></div><aside class="stat"><b>NOT ANOTHER EVENT LIST</b><p>Every result explains why leaving your room is worth it — and Okupy can text you a nudge before it starts.</p></aside></section>
<div class="grid"><section class="card"><h2>01 / Agent memory</h2><form id="profile">
<label for="uid">Your private ID or phone</label><input id="uid" required maxlength="256" autocomplete="username" placeholder="+1 415…">
<label for="project">What are you building?</label><textarea id="project" required maxlength="2000" placeholder="An API that…"></textarea>
<label for="location">City</label><input id="location" required maxlength="240" placeholder="San Francisco, CA">
<label for="goals">Who or what do you need?</label><input id="goals" maxlength="1000" placeholder="cofounder, first customers, product feedback">
<button type="submit">Remember me</button></form></section>
<section class="card"><h2>02 / Connections</h2><p>Composio handles authorization. Okupy never asks for your Gmail password.</p><button class="alt" id="gmail" type="button">Connect Gmail ↗</button>
<h2 style="margin-top:35px">03 / Find a room</h2><label for="ask">What should I find?</label><input id="ask" placeholder="Find something this Thursday"><button id="discover" type="button">Search with Firecrawl</button><div id="status" aria-live="polite"></div></section></div>
<section class="card"><h2>04 / Scheduled reminders</h2><p>Set a reminder in chat ("remind me in 4 hours" or "ping me 12h before") and Okupy will text you when it fires.</p><div id="reminders" aria-live="polite"></div></section>
<section class="card"><h2>05 / Watch cart — Jumia Ghana + Amazon</h2><p>Paste a Jumia Ghana (jumia.com.gh) or Amazon product URL and Okupy will track it. When the price drops — or hits a target you set — it texts you on iMessage.</p>
<form id="watchForm" style="display:grid;grid-template-columns:1fr 130px 110px;gap:10px;align-items:end">
<div><label for="watchUrl">Product URL (Amazon or Jumia)</label><input id="watchUrl" required type="url" placeholder="jumia.com.gh/…-<ID>.html or amazon.com/dp/…"></div>
<div><label for="watchTarget">Target price</label><input id="watchTarget" type="text" inputmode="decimal" placeholder="199.99"></div>
<div><button type="submit" style="margin-top:0">Watch</button></div>
</form>
<div class="cart-controls"><button class="ghost" id="refreshCart" type="button">Refresh prices</button><span id="cartMeta" style="font-size:12px;color:#444"></span></div>
<div id="cart" aria-live="polite"></div></section>
<div class="steps"><div><b>MEMORY</b>Your build context follows you.</div><div><b>SEARCH</b>Live, sourced Firecrawl results.</div><div><b>REMIND</b>Timed iMessage nudges.</div><div><b>WATCH</b>Jumia Ghana + Amazon price-drop alerts.</div><div><b>CONNECT</b>Composio powers your apps.</div></div></main>
<script>
const $=id=>document.getElementById(id), status=$('status');
async function request(path, options){const response=await fetch(path,options);const text=await response.text();let body;try{body=JSON.parse(text)}catch{throw new Error(text||'The server returned an unreadable response.')}if(!response.ok)throw new Error(body.error||'Request failed.');return body}
function values(){return {userId:$('uid').value.trim(),project:$('project').value.trim(),location:$('location').value.trim(),goals:$('goals').value.split(',').map(value=>value.trim()).filter(Boolean),interests:[]}}
async function loadReminders(){const userId=$('uid').value.trim();const target=$('reminders');if(!userId){target.innerHTML='<p style="font-size:13px;margin-top:10px">Save your profile above to view scheduled reminders.</p>';return}try{const body=await request('/v1/reminders/'+encodeURIComponent(userId));if(!body.reminders||body.reminders.length===0){target.innerHTML='<p style="font-size:13px;margin-top:10px">No reminders yet. Ask the agent to set one.</p>';return}target.innerHTML=body.reminders.map(reminder=>{const at=new Date(reminder.fireAt).toLocaleString();const event=reminder.eventTitle?reminder.eventTitle:'';return '<div class="reminder" data-id="'+reminder.id+'"><div><time>'+at+'</time><b>'+reminder.message+'</b>'+(event?'<div style="font-size:12px">'+event+'</div>':'')+'</div><button data-cancel="'+reminder.id+'">Cancel</button></div>'}).join('');for(const button of target.querySelectorAll('button[data-cancel]')){button.onclick=async()=>{try{await request('/v1/reminders/'+button.dataset.cancel+'?userId='+encodeURIComponent(userId),{method:'DELETE'});await loadReminders()}catch(error){status.textContent='Cancel failed. '+error.message}}}}
}catch(error){target.innerHTML='<p style="font-size:13px;margin-top:10px">Reminders unavailable: '+error.message+'</p>'}}
function money(value,currency){const parsed=Number(value);if(!Number.isFinite(parsed))return value+' '+currency;const formatted=parsed.toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2});const symbol=currency==='USD'?'$':currency==='GHS'?'GH₵ ':'';return symbol?symbol+formatted:formatted+' '+currency}
async function loadCart(){const userId=$('uid').value.trim();const target=$('cart');const meta=$('cartMeta');if(!userId){target.innerHTML='<div class="watch-empty">Save your profile above to start tracking items.</div>';meta.textContent='';return}try{const body=await request('/v1/cart/'+encodeURIComponent(userId)+'?alerts=5');const items=body.items||[];const active=items.filter(i=>i.status==='active');meta.textContent=active.length+' active · '+items.filter(i=>i.status==='paused').length+' paused';if(items.length===0){target.innerHTML='<div class="watch-empty">Your watch cart is empty. Paste a jumia.com.gh or Amazon URL above to add an item.</div>';return}const alerts=body.alerts||[];const alertsHtml=alerts.length?'<div style="margin-top:14px;border-top:1.5px solid;padding-top:10px"><b style="font:13px system-ui">Recent alerts</b>'+alerts.map(a=>'<div style="font-size:12px;margin-top:4px;padding:6px;border-left:3px solid var(--warn);background:#fff8f8">'+new Date(a.deliveredAt).toLocaleString()+' · '+a.alertKind+' · '+(a.newPrice?money(a.newPrice,a.currency||'USD'):'')+'</div>').join('')+'</div>':'';target.innerHTML=items.map(item=>{const img=item.imageUrl?'<img src="'+item.imageUrl+'" alt="">':'<img src="" alt="" style="background:#eee">';const last=item.lastPrice?money(item.lastPrice,item.lastCurrency):'—';const target=item.targetPrice?money(item.targetPrice,item.lastCurrency):null;const initial=item.initialPrice?money(item.initialPrice,item.lastCurrency):null;const dropped=initial&&Number(item.lastPrice)<Number(item.initialPrice);const reachedTarget=item.targetPrice&&item.lastPrice&&Number(item.lastPrice)<=Number(item.targetPrice);const priceClass=reachedTarget?'target-hit':dropped?'drop':'';const statusLabel=item.status==='paused'?' (paused)':item.status==='removed'?' (removed)':'';const alertLine=reachedTarget?'<div class="alert">★ Target hit</div>':dropped?'<div class="alert">↓ Dropped from '+initial+'</div>':'';const checked=item.lastCheckedAt?new Date(item.lastCheckedAt).toLocaleString():'never';return '<div class="watch-item" data-id="'+item.id+'">'+img+'<div class="info"><b>'+item.title+'</b><div class="meta">'+(item.store==='jumia'?'Jumia GH · SKU ':'Amazon · ASIN ')+item.asin+' · checked '+checked+statusLabel+'</div><div class="price '+priceClass+'">'+last+'</div>'+(target?'<div class="target">Target: '+target+'</div>':'')+alertLine+'</div><div class="actions">'+(item.status==='active'?'<button data-pause="'+item.id+'">Pause</button>':'<button data-resume="'+item.id+'">Resume</button>')+'<button data-remove="'+item.id+'">Remove</button><a class="ghost" style="padding:6px 10px;font-size:11px;text-decoration:none;border:1.5px solid;display:inline-block;text-align:center" href="'+item.url+'" target="_blank" rel="noopener">View</a></div></div>'}).join('')+alertsHtml;for(const button of target.querySelectorAll('button[data-remove]')){button.onclick=async()=>{try{if(!confirm('Remove this item from your watch cart?'))return;await request('/v1/watch/'+button.dataset.remove+'?userId='+encodeURIComponent(userId),{method:'DELETE'});await loadCart()}catch(error){status.textContent='Remove failed. '+error.message}}}
for(const button of target.querySelectorAll('button[data-pause]')){button.onclick=async()=>{try{await request('/v1/watch/'+button.dataset.pause+'/pause',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({userId})});await loadCart()}catch(error){status.textContent='Pause failed. '+error.message}}}
for(const button of target.querySelectorAll('button[data-resume]')){button.onclick=async()=>{try{await request('/v1/watch/'+button.dataset.resume+'/resume',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({userId})});await loadCart()}catch(error){status.textContent='Resume failed. '+error.message}}}}
}catch(error){target.innerHTML='<div class="watch-empty">Cart unavailable: '+error.message+'</div>'}}
$('profile').onsubmit=async event=>{event.preventDefault();status.textContent='Saving…';try{const profile=values();await request('/v1/profile/'+encodeURIComponent(profile.userId),{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify(profile)});status.textContent='✓ Memory saved. Text the agent whenever you want.';await loadReminders();await loadCart()}catch(error){status.textContent='Could not save: '+error.message}};
$('discover').onclick=async()=>{status.textContent='Searching the city…';try{const body=await request('/v1/discover',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({userId:$('uid').value.trim(),message:$('ask').value.trim()||'Find me an event'})});status.textContent=body.reply;await loadReminders()}catch(error){status.textContent='Search failed. '+error.message+' Please try again.'}};
$('gmail').onclick=async()=>{status.textContent='Starting Gmail connection…';try{const body=await request('/v1/connections',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({userId:$('uid').value.trim()||'dashboard-user',app:'gmail'})});if(body.redirectUrl)location.assign(body.redirectUrl);else status.textContent=body.note}catch(error){status.textContent='Connection failed. '+error.message}};
$('watchForm').onsubmit=async event=>{event.preventDefault();const userId=$('uid').value.trim();const url=$('watchUrl').value.trim();const targetRaw=$('watchTarget').value.trim();if(!userId){status.textContent='Add your user ID first.';return}status.textContent='Adding to watch cart…';try{const payload={userId,url,targetPrice:targetRaw||null};const body=await request('/v1/watch/'+encodeURIComponent(userId),{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});status.textContent=body.reply||'✓ Watching.';$('watchUrl').value='';$('watchTarget').value='';await loadCart()}catch(error){status.textContent='Watch failed. '+error.message}};
$('refreshCart').onclick=async()=>{const userId=$('uid').value.trim();if(!userId){status.textContent='Add your user ID first.';return}status.textContent='Refreshing prices…';try{await request('/v1/watch/'+encodeURIComponent(userId)+'/check',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({userId})});await loadCart();status.textContent='✓ Prices refreshed.'}catch(error){status.textContent='Refresh failed. '+error.message}};
$('uid').addEventListener('change',()=>{loadReminders();loadCart()});
loadReminders();
loadCart();
</script></body></html>`;
